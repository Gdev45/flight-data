[project]
name = "flight-data"
version = "0.1.0"
description = "Terminal tool to fetch and display flight data"
requires-python = ">=3.8"
dependencies = [
    "requests",
    "rich"
]

[project.scripts]
flight-data = "main:main"
